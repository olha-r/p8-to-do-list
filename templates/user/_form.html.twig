{{ form_start(form) }}
    {{ form_widget(form) }}
{#<button type="submit" class="btn btn-success pull-right">Modifier</button>#}
    <button class="btn btn-success pull-right">{{ button_label|default('Créer') }}</button>
{{ form_end(form) }}
